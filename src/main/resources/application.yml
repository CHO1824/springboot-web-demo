server:
  port: 8085

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/shop?useUnicode=true&characterEncoding=utf8mb4
    username: shopuser
    password: shop1234
    driver-class-name: org.mariadb.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.example.demo.dto